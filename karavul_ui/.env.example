# KARAVUL — Taktiksel Komuta Merkezi
# Bu dosyayı `.env` olarak kopyalayıp gerekirse değiştirin. Varsayılan,
# `api.py`nin `uvicorn api:app --reload --port 8000` ile çalıştığı port'tur.
VITE_API_BASE_URL=http://localhost:8000
